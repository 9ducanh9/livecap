# LiveCap Backend dependencies (Python 3.11+)

# Web framework + ASGI server
fastapi==0.115.0
uvicorn[standard]==0.30.6

# AWS SDK + Amazon Transcribe Streaming
boto3==1.35.24
amazon-transcribe==0.6.2

# CloudWatch logging handler
watchtower==3.2.0

# Optional AWS X-Ray tracing (enabled via ENABLE_XRAY)
aws-xray-sdk==2.14.0

# Configuration via environment variables
python-dotenv==1.2.2
