# LiveCap Backend dependencies (Python 3.11+)

# Web framework + ASGI server
fastapi==0.115.0
uvicorn[standard]==0.30.6

# AWS SDK + Amazon Transcribe Streaming
boto3==1.35.24
amazon-transcribe==0.6.2

# CloudWatch logging handler
watchtower==3.2.0

# Optional AWS X-Ray tracing (enabled via ENABLE_XRAY)
aws-xray-sdk==2.14.0

# Optional Stripe subscription billing (enabled via ENABLE_STRIPE_BILLING)
stripe==11.6.0

# Optional DeepSeek meeting-summary integration (enabled via ENABLE_MEETING_SUMMARY)
httpx==0.27.2

# Configuration via environment variables
python-dotenv==1.2.2
